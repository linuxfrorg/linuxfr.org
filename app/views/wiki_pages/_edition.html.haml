#wiki_edition
  %h1
    Éditions
  %p
    = link_to "Éditer", edit_wiki_page_path(edition)
  %p
    Liste des dernières modifications :
  %ul
    = list_of(edition.versions) do |v|
      = link_to "#{v.user.try :name} : #{v.message}", wiki_page_revision_path(edition.friendly_id, v.version)
