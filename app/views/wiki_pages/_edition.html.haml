#wiki_edition
  %h1
    Éditions
  %p
    = link_to "Éditer", edit_wiki_page_path(edition)
  %p
    Liste des dernières modifications :
  %ul
    - edition.revisions.each do |v|
      %li
        = link_to "#{h v.committer.name} : #{h v.message}", wiki_page_show_diff_path(edition.id, v.sha)
