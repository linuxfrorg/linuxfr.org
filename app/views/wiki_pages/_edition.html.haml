#wiki_edition.box
  %h1
    Éditions
  %p
    = link_to "Éditer", "/wiki/#{edition.to_param}/modifier"
  %p
    Modifié le
    = l edition.versions.first.created_at.to_date
  %p
    Les dernières modifications&nbsp;:
  %ul
    = list_of(edition.versions) do |v|
      = link_to "#{v.user.try :name}&nbsp;: #{v.message}".html_safe, "/wiki/#{edition.to_param}/revisions/#{v.version}"
