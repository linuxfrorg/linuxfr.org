- title @wiki_page.title
- meta_for @wiki_page
- if @wiki_page.title == WikiPage::HomePage
  - feed "Flux Atom du wiki", "/wiki.atom"

= content_for :column do
  = render 'edition', :edition => @wiki_page

= render @wiki_page, :full => true
= render @wiki_page.node
