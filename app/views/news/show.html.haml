- title @news.title
- cache_unless account_signed_in?, "news/show/#{@news.to_param}", expires_in: 1.minute do
  %main.measure#contents(role="main")
    - meta_for @news
    = render @news, with_second_part: true
    = render @news.node
