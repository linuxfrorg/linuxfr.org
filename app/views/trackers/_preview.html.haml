- article_for preview do
  %header
    %h2
      = link_to "#{h preview.category_title} : #{h preview.title}", preview
    %p
      = posted_by(preview)
  .content
    = sanitize(spellify preview.body)
  %footer
    %p
      = read_it(preview)
