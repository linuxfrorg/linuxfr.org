<% @boards.reverse_each do |board| %>
<%= board.id %>	<%= board.created_at.to_s(:timestamp) %>	<%= raw board.remove_control_chars board.user_agent %>	<%= board.user_name %>	<%= raw board.remove_control_chars board.message %>
<% end %>
