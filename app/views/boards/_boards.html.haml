- board = @boards.build
.board{'data-chat' => board.private_key}
  - if current_account && current_account.can_post_on_board?
    = render 'boards/form', :form => board
  .inbox
    = render :partial => 'boards/board', :collection => @boards
