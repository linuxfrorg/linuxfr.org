%h4
  = link_to "Commentaire : #{comment.title}", [comment.node, comment]
.content
  = comment.body
%p
  = "Score : #{comment.score}"
- if logged_in? && comment.votable_by?(current_user)
  - form_tag relevance_url(:action => 'for', :comment_id => comment.id) do
    = submit_tag "Pertinent"
  - form_tag relevance_url(:action => 'against', :comment_id => comment.id) do
    = submit_tag "Inutile"
%p
  = link_to "Répondre", answer_comment_url(:node_id => comment.node_id, :parent_id => comment.id)
