%h2
  Vos télégrammes en attente de modération
- if @news.empty?
  Vous n'avez aucune télégramme en cours de modération
- else
  %table#my_trackers
    %tr
      %th Section
      %th Sujet
      %th Date de soumission
    - @news.each do |news|
      %tr
        %td= news.section.title
        %td= news.title
        %td= l news.node.created_at
