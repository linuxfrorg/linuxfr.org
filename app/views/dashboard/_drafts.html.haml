%h2
  Vos dépêches dans l'espace de rédaction
- if @drafts.empty?
  Vous n'êtes à l'origine d'aucune dépêche dans l'espace de rédaction.
- else
  %table#my_drafts
    %tr
      %th Section
      %th Sujet
      %th Date de création
    - @drafts.each do |news|
      %tr
        %td= news.section.title
        %td= link_to news.title, [:redaction, news]
        %td= l news.created_at
