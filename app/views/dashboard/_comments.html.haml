%h2
  Vos derniers commentaires
- if @comments.empty?
  %p
    Vous n'avez posté aucun commentaire
- else
  %table#my_comments
    %tr
      %th Fil initiateur
      %th Sujet du commentaire
      %th Date
      %th Note
      %th Nb de réponses
      %th Dernière réponse
    - @comments.each do |comment|
      - answer = comment.last_answer
      %tr
        %td #{translate_content_type comment.content_type} : #{link_to_content comment.content}
        %td= link_to comment.title, url_for_content(comment.node.content) + "#comment-#{comment.id}"
        %td= comment.created_at.to_s(:posted)
        %td= comment.score
        %td= comment.nb_answers
        %td= answer && answer.created_at.to_s(:posted)
