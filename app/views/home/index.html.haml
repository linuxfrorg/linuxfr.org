- content_for :column do
  = render 'polls/box'

=h1 "Accueil"
- feed "Flux Atom des télégrammes", "/news.atom"
- if @ppp
  %section#phare
    = render @ppp
- if @banner
  %aside.banner
    = @banner
= paginated_section @nodes, link_to("Proposer un télégramme", "/news/nouveau") do
  %main#contents(role="main")
    = render @nodes.map(&:content)
