= article_for preview do |c|
  - c.title = "Journal : #{h preview.title}"
  - c.image = image_tag(current_user.avatar.url(:thumbnail), :class => 'avatar')
  - c.body  = sanitize(spellify preview.body)
