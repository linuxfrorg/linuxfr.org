- content_for :column do
  = render 'recent'
  = render 'nodes'

=h1 "#{@user.name} a écrit #{pluralize @nodes.total_count, "post"}"
- feed "Flux Atom des posts de #{@user.name}"
= paginated_nodes @nodes
