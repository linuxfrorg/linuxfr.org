- content_for :column do
  = render 'recent'
  = render 'nodes'

=h1 "#{@user.name} a écrit #{pluralize @nodes.total_entries, "journal"}"
- feed "Flux Atom des journaux de #{@user.name}"
= paginated_nodes @nodes
