- content_for :column do
  = render 'recent'
  = render 'nodes'

=h1 "#{@user.name} a écrit #{pluralize @nodes.total_entries, "dépêche"}"
- feed "Flux Atom des dépêches de #{@user.name}"
= paginated_nodes @nodes
