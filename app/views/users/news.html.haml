- content_for :column do
  = render 'recent'
  = render 'nodes'

=h1 "#{@user.name} a écrit #{pluralize @nodes.total_count, "télégramme"}"
- feed "Flux Atom des télégrammes de #{@user.name}"
= paginated_nodes @nodes
