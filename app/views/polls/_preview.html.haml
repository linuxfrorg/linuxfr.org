- article_for preview do
  %footer
    %h2
      = "Sondage : #{h preview.title}"
    %p
      = posted_by(preview)
  .content
    = render "answers_current", :poll => preview
  %footer
    %p
      = read_it(preview)
