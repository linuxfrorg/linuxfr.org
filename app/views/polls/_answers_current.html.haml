= form_tag "/sondages/#{poll.id || 0}/vote" do
  %ul
    = list_of poll.answers do |answer|
      = radio_button_tag :position, answer.position, false
      = label_tag(:position, answer.answer)
  = submit_tag "Voter" unless poll.new_record?
