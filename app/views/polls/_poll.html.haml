- article_for poll do
  %h3
    = link_to "Sondage : #{poll.title}", poll
  %p
    = posted_by(poll)
  .content
    = render "polls/answers_#{poll_current_or_archived poll}", :object => poll
  %p
    = read_it(poll)
