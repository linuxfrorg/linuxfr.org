- article_for poll do
  %header
    %h2
      = link_to "Sondage : #{poll.title}", moderation_poll_path(:id => poll.id)
    %p
      = posted_by(poll)
  .content
    = render "polls/answers_current", :poll => poll
  %footer
    %p
      = read_it(poll)
