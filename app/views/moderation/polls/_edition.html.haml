%article
  %h2 Éditions
  %p
    = link_to "Éditer", edit_moderation_poll_path(:id => @poll.id)
  %h2 Modération
  - form_tag accept_moderation_poll_path(@poll) do
    = submit_tag "Publier"
  - form_tag refuse_moderation_poll_path(@poll) do
    = submit_tag "Refuser"
