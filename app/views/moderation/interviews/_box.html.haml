#interviews_box
  %h1 Entretiens
  %p
    - if @interviews.empty?
      Aucune proposition
    - else
      - @interviews.each do |interview|
        = link_to interview.title, [:moderation, interview]

