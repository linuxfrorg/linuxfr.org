%main.devise.measure#contents(role="main")
  %a.devise.logo(href="/")
    %img{src: Logo.image, title: "LinuxFr.org"}
    %hr
  =h1 "Renvoyer les instructions de confirmation"

  = form_for @account, url: confirmation_path(:account) do |f|
    = messages_on_error @account
    %p
      = f.email_field :email, placeholder: "Adresse de courriel"
    %p
      = f.submit "Envoyer"

  .devise.links
    = render partial: "devise/shared/links"
