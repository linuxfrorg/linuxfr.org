%aside#sidebar
  %header#branding
    = logo

  .login.box
    :javascript
      if (location.protocol == 'http:') {
        document.write('<a href="https://' + location.hostname + location.pathname + '"><img alt="Accès en clair (http)" class="access" src="/images/icones/lock-insecure.png" /></a>');
      } else {
        document.write('<img alt="Accès sécurisé (https)" class="access" src="/images/icones/lock-secure.png" />');
      }
    - if account_signed_in?
      %h1= current_user.name
      %p Il vous reste #{current_user.account.nb_votes} avis
      %p= link_to "Proposer un contenu", '/proposer-un-contenu'
      %p= link_to "Mon tableau de bord", '/tableau-de-bord'
      %p= link_to "Mes contenus taggés", '/tags'
      %p= link_to "Modifier mes préférences", '/compte/modifier'
      %p= link_to "Changer de style", '/stylesheet/modifier'
      %p= link_to "Se déconnecter", '/compte/deconnexion'
    - else
      %h1 Se connecter
      = render "sessions/new", :id_suffix => "_sidebar"
      %p= link_to "Pas de compte ? S'inscrire !", '/compte/inscription'
      %p= link_to "Proposer un contenu", '/proposer-un-contenu'
  - if current_user && current_user.admin?
    = render 'admin/box'
  - if current_user && current_user.amr?
    = render 'moderation/box'
  - if current_user && (current_user.writer? || current_user.amr?)
    = render 'redaction/box'
  = yield :column
